
throw error daca o directiva nu exista-> ba nu, scoate attr

trait care face parse trb sa fie un factory initiat de la bun inceput si el va delega tot cu config cu orice, va tine loc si la process si muta templ fn
ba nu, va fi pe abstract entity, voi avea un entity Root care e chemat din afara primul, toate petodele vor fi protected si voi putea avea event uri de before si after

pune facadele la punct
un main app pe fiecare
phpt laravelserviceprovider
un bootstrap care sa buteze facadele cu application

template::config()
new template(src dest, options)
template addpath(alias, src, options)
template adddirective(?alias, name, fn)

la final de render trb resetate staticele ca va face conflict modular

nested oplon variation cu reset la final

repara playground

de prevenit recursivitati pe insert nodes
query selectors si spatiile in [attr=val val] => cauta intai match exact, apoi spart, nicidecum spart amandoua
