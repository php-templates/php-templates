documentat ca nu recomand deloc folosirea tagurilor pure de php!!!! am bagat tag-uri <php></php>
sintaxa 
<php p-if="foo">
    string
</php>
<php if >

dar si 
<php>
php here
</php>

trebuie rezolvat cu binds from slot to surface... poate fac o alta expresie pt asta... wrap in <template>

pune directivele custom pan la urma...

template::config()
template::srcpath multiplu, de gasit varianta cea mai ok, cel mai ok ar fi daca as suprascrie o functie, un loader care sa imi intoarca path ul
dar nu as mai avea un default ca fallback... 
sau si.si iar cand zic src_path, zic default path

se va ocupa un alt tool de pus if-uri, tot el face handle la custom directives si asa am centralizat locul unde se fac astea

<lreplace final de php tags open si close la elseif se poate evita

teste noua implementare directives
parseNode va fi pe un trait folosit de toti
func lui entities/templ vor fi cele ale lui AbstractEntity
func lui entities/Tenplate vor fi preluate de new Context()->parse()
func din abstract entites ce tin de pregatire fisier pt parsare vor fi in new Context
scos taguri <php> si inlocuit cu blade syntax
pregatire templ implica: replcallback pentru @php, php blocks to store in to be replaced late
replace urile nu trb sa fie per thread, ci globale, altfel nested comp nu vor avea acces la ele. Vor fi procesate o singura data pe doc render
niciun tag <> in html pan la final

in final, suport blade syntax if
