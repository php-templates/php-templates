
throw error daca o directiva nu exista
phpt laravelserviceprovider

solve all todos
document with no dest file

verbatim no parse node

fa ceva cu Helper mare

fn in loc de sldft

scope data trecute pe slot pe $_scope validare in caz de erori

init like this:
Config $cfg (alias, src)
ConfigHolder $cfgh ($defaultcfg, destpath)
$cfgh->add()  cfg may access ownerConfigholder
new Document(name, new DependenciesMap(path))
new ViewFactory(document, new viewParser(document, cfgh, eventholder))

larafacades
Template::addConfig()
Template::on()
Template::getConfig()->directive()
Template::directive()

on error
$e->setSourceContext($source);

domnodeattribute implements domnoneattrinterface

static glob all files from entities si fiecare face test pe node
playground bootstrap
func get arg(0)
entity numita <extends template=""> care primeste nu new context, ci context actual
un mecanism care sa faca teste doar mixed automated